# 评测约定

默认采用 leave-one-out 的时间切分：每个用户最后一次交互作为测试样本，倒数第二次作为验证样本，其余作为训练样本。若项目使用其他切分方式，必须在报告中说明原因。

## 指标

- Recall@K：真实目标是否出现在前 K 个推荐中
- HitRate@K：对单目标测试样本，Recall@K 的等价表达
- NDCG@K：同时考虑命中与排名位置
- Coverage：被推荐到的不同物品比例
- Diversity：推荐列表内部的差异程度

报告中至少统一给出 K=5、10、20 的结果，并明确候选集、负样本数量和过滤已交互物品的规则。
